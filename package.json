{
  "name": "fullstack-app",
  "packageManager": "yarn@4.6.0",
  "scripts": {
    "setup:environment": "yarn workspace backend migrate && yarn workspace backend seeder:run",
    "dev": "concurrently \"yarn workspace backend start:dev\" \"yarn workspace frontend dev\"",
    "build": "yarn workspace backend build && yarn workspace frontend build",
    "test": "yarn workspace backend test && yarn workspace backend test:e2e && yarn workspace frontend test"
  },
  "workspaces": [
    "frontend",
    "backend"
  ],
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "dependencies": {
    "corepack": "^0.32.0"
  }
}
